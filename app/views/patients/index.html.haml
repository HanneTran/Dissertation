-# Author: Hanne Tran
-# Date: 20.04.19 
-# Page with search box and list of all patients in the system.


.col-xs-5
  = simple_form_for :search, url: search_patients_path, method: :post do |f|
    = f.input :name, label: 'Search by name:'
    = f.submit 'Search', class: 'btn btn-primary btn-md'
    = link_to 'Reset', patients_path
  %br 

%p There are #{@patients.size} patients in the database.
%table.table.table-hover
  %thead
    %tr
      %th Name
      %th Category
  %tbody
    - @patients.each do |patient|
      %tr
        %td= patient.name
        %td= patient.category_type
        %td= link_to 'Show', patient


