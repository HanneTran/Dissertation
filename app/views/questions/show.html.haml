%p#notice= notice

%h1 Question #{@question.id}: #{@question.description}
%div{:align => "center"}
%p The file name: #{@q.file}, patient ID: #{@q.patient_id}, question ID #{@q.question_id}
.video
  = video_tag "#{@q.file}", :size => "600x600", :controls => true, :style => "float: left; margin-left: 100px"
=link_to 'Back', patient_questions_path, class: 'btn btn-primary btn-lg'
